# Needed for actual model calls (verifying the response content). The bug
# itself is about which system prompt version gets sent to the model, so a
# real key is needed to see the reply, though the mismatch is also visible
# directly on the server (see README "Verifying without a model call").
OPENAI_API_KEY=sk-your-real-key-here
